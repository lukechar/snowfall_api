The snowfall_api scrapes web results to get the latest snow and weather conditions for various ski resorts in the US + Canada.

The API endpoints are accessible with the URI format: "/snow_report/[resort name]"
